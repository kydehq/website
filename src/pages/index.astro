---
import Base from "../layouts/Base.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import ArchitectureDiagram from "../components/ArchitectureDiagram.astro";
---

<Base
    title="KYDE — Identity and Control for the Agentic Economy."
    description="KYDE provides identity and control for the agentic economy — cryptographic agent identity, role boundaries, budget enforcement, and tamper-evident audit trails for every agent action."
>
    <Navbar />

    <main>
        <!-- ══════════ HERO STAGE (viewport) ══════════ -->
        <section class="relative overflow-hidden h-screen min-h-[600px] flex flex-col justify-center pt-20">
            <!-- Background video -->
            <video class="absolute inset-0 w-full h-full object-cover hero-video" muted loop playsinline poster="/stage-poster.jpg">
                <source src="/stage-video.mp4" type="video/mp4" />
            </video>
            <!-- Gradient overlay -->
            <div class="absolute inset-0 bg-gradient-to-b from-[#0d0e10]/30 via-[#0d0e10]/60 to-[#0d0e10]/90"></div>
            <!-- Noise texture -->
            <div class="absolute inset-0 pointer-events-none hero-noise"></div>
            <div class="bg-grid absolute inset-0 pointer-events-none opacity-20"></div>
            <!-- Corner vignette — covers bottom-right, looks cinematic -->
            <div class="absolute inset-0 pointer-events-none" style="background: radial-gradient(ellipse 40% 35% at 100% 100%, #0d0e10 0%, transparent 100%);"></div>

            <div class="relative max-w-7xl mx-auto px-6 w-full">
                <!-- compliance label -->
                <div class="flex items-center gap-3 mb-6">
                    <span class="h-1.5 w-1.5 rounded-full bg-[#fcb830] shrink-0"></span>
                    <span class="font-mono text-xs uppercase tracking-[0.2em] bg-[#dae4e5] text-[#0d0e10] px-1.5 py-0.5">↳ NIS-2 · DORA · EU AI Act · GDPR — Audit-Ready from Day One</span>
                </div>
                <!-- Headline — two separate lines so width changes never cause reflow -->
                <h1 class="hero-h1 text-6xl text-white uppercase tracking-tighter md:text-7xl lg:text-8xl" style="opacity:0;animation:heroFadeIn 0.3s ease-out 0.1s forwards;">
                    <span id="hero-l1" style="display:block">Identity and Control</span>
                    <span id="hero-l2" style="display:block">for the Agentic Economy.</span>
                </h1>
            </div>
        </section>

<style>
  @keyframes heroFadeIn {
    to { opacity: 1; }
  }
</style>

<script>
  const CHARS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
  const SPEED = 0.22;

  function scrambleLine(el: HTMLElement, target: string, delay: number) {
    let frame = 0;
    setTimeout(() => {
      // Measure the height of the FINAL text first, then lock to it
      // so the container never jumps when scramble resolves
      el.innerText = target;
      const finalH = el.offsetHeight;
      el.style.height = finalH + "px";
      el.style.overflow = "hidden";

      function tick() {
        let out = "";
        for (let i = 0; i < target.length; i++) {
          if (i < frame) {
            out += target[i];
          } else if (target[i] === " " || target[i] === ".") {
            out += target[i];
          } else {
            out += CHARS[Math.floor(Math.random() * CHARS.length)];
          }
        }
        el.innerText = out;
        frame += SPEED;
        if (frame < target.length) {
          requestAnimationFrame(tick);
        } else {
          el.innerText = target;
          el.style.height = "";
          el.style.overflow = "";
        }
      }
      tick();
    }, delay);
  }

  const l1 = document.getElementById("hero-l1") as HTMLElement;
  const l2 = document.getElementById("hero-l2") as HTMLElement;
  if (l1) scrambleLine(l1, "IDENTITY AND CONTROL", 200);
  if (l2) scrambleLine(l2, "FOR THE AGENTIC ECONOMY.", 200);

  // Autoplay via JS to pass HTML validator (autoplay attr not allowed per spec)
  const vid = document.querySelector(".hero-video") as HTMLVideoElement;
  if (vid) vid.play().catch(() => {});
</script>

        <!-- ══════════ HERO INTRO ══════════ -->
        <section class="bg-[#f1f3f5] border-b border-dotted border-[#0d0e10]/15 py-20 md:py-28">
            <div class="max-w-7xl mx-auto px-6">
                <div class="grid md:grid-cols-[200px_1fr] gap-8 items-start">
                    <div>
                        <p class="text-xs font-bold uppercase tracking-[0.15em] text-[#0d0e10]/60">
                            Securing the Agentic Economy
                        </p>
                        <p class="mt-2 font-mono text-xs text-[#0d0e10]/40">[KYDE]</p>
                    </div>
                    <div class="space-y-10">
                        <!-- Subline -->
                        <p class="text-xl md:text-2xl uppercase tracking-wide leading-relaxed text-[#0d0e10]">
                            AI agents are calling APIs, moving money, making decisions - with no identity, no scope, no audit trail. When something goes wrong, no one knows who acted, why, or under what limits.
                        </p>
                        <p class="text-xl md:text-2xl uppercase tracking-wide leading-relaxed text-[#0d0e10] font-bold">
                            KYDE is the gateway between agents and the world. Agents will rewrite what they can access. KYDE sits where they can't.
                        </p>
                        <p class="text-xl md:text-2xl uppercase tracking-wide leading-relaxed text-[#0d0e10]">
                            Every action is logged and checked against policy before it reaches critical systems - blocked when out of scope. Deployed via a single environment variable. No code changes required.
                        </p>

                        <!-- Terminal -->
                        <div class="border border-dotted border-[#0d0e10]/20 bg-white">
                            <div class="flex items-center gap-2 border-b border-dotted border-[#0d0e10]/15 px-4 py-3">
                                <span class="h-3 w-3 rounded-full bg-[#c00000]/40"></span>
                                <span class="h-3 w-3 rounded-full bg-[#fcb830]/60"></span>
                                <span class="h-3 w-3 rounded-full bg-[#0a7c4e]/40"></span>
                                <span class="ml-2 text-xs text-[#888] font-mono">terminal</span>
                            </div>
                            <div class="p-5 font-mono text-sm leading-relaxed">
                                <p class="text-[#555]">
                                    <span class="text-[#0a7c4e]">$</span>
                                    <span class="text-[#0d0e10]"> export OPENAI_BASE_URL=https://kyde.intranet/v1</span>
                                </p>
                                <p class="mt-1 text-[#555]">
                                    <span class="text-[#0a7c4e]">$</span>
                                    <span class="text-[#0d0e10]"> kyde fleet init</span>
                                </p>
                                <p class="mt-1 text-[#4472c4]">&#10003; Fleet proxy started on :8080</p>
                                <p class="text-[#4472c4]">&#10003; Agent identities provisioned</p>
                                <p class="text-[#4472c4]">&#10003; Role boundaries enforced</p>
                                <p class="text-[#4472c4]">&#10003; Tamper-evident ledger initialized</p>
                                <p class="mt-2 text-[#555]">
                                    <span class="text-[#0a7c4e]">$</span>
                                    <span class="text-[#0d0e10]"> kyde fleet status</span>
                                </p>
                                <p class="mt-1 text-[#0a7c4e]">&#10003; 12 agents active — all scoped, signed, accountable</p>
                            </div>
                        </div>

                        <!-- Trust markers -->
                        <div class="flex flex-wrap gap-x-8 gap-y-2 font-mono text-xs uppercase tracking-widest text-[#0d0e10]/40">
                            <span>One ENV Variable</span>
                            <span>Target &lt;100ms Latency</span>
                            <span>Provider-Agnostic</span>
                            <span>MCP-Ready</span>
                            <span>Zero Code Changes</span>
                        </div>

                        <!-- CTAs -->
                        <div class="flex flex-col sm:flex-row items-start gap-4">
                            <a
                                href="#problem"
                                class="inline-flex items-center gap-2 bg-[#0d0e10] rounded-full px-6 py-3.5 text-xs font-mono uppercase tracking-[0.1em] text-white hover:bg-[#fcb830] hover:text-[#0d0e10] transition-all"
                            >
                                See why this matters →
                            </a>
                            <button
                                onclick="openModal()"
                                class="inline-flex items-center gap-2 border border-[#0d0e10]/30 rounded-full px-6 py-3.5 text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10] hover:border-[#0d0e10] transition-all"
                            >
                                Book Demo
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- ══════════ PROBLEM ══════════ -->
        <section id="problem" class="relative border-t border-dotted border-[#0d0e10]/20 py-24 md:py-32">
            <div class="relative max-w-7xl mx-auto px-6">
                <div class="max-w-3xl mx-auto text-center mb-16">
                    <p class="mb-6 font-mono text-xs uppercase tracking-[0.2em] bg-[#dae4e5] text-[#0d0e10] px-1.5 py-0.5 w-fit mx-auto">↳ The operational gap</p>
                    <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight text-[#0d0e10] uppercase">
                        Questions most enterprises can't answer.
                    </h2>
                </div>

                <div class="grid gap-6 md:grid-cols-2">
                    {[
                        {
                            q: "Who acted?",
                            body: "Agents share service accounts with broad permissions. No traceable identity. No role boundaries. A support agent scoped for €500 refunds issues €5,000 because nobody scoped its authority.",
                            label: "Identity",
                        },
                        {
                            q: "Why did it decide that?",
                            body: "An HR agent retrieves a policy document from 2022 and uses it to guide a termination. The current policy says something different. Nobody logged which source it relied on.",
                            label: "Causal Context",
                        },
                        {
                            q: "What are the boundaries?",
                            body: "Token budgets don't exist. API access is unrestricted. An intern's research agent burns through your entire monthly token allocation while your production agents queue.",
                            label: "Scope & Budget",
                        },
                        {
                            q: "What happened?",
                            body: "A procurement agent posts confidential contract terms in Slack. Leadership needs the full evidence trail. The logs are vendor-provided, self-reported, software-only. A compromised host can rewrite them silently.",
                            label: "Tamper-Evidence",
                        },
                    ].map(({ q, body, label }) => (
                        <div class="border border-[#e5e7eb] rounded-[22px] p-8 bg-white transition-all hover:border-[#0d0e10]/30">
                            <div class="mb-4 flex items-center justify-between">
                                <span class="font-mono text-xs uppercase tracking-widest text-[#333]">{label}</span>
                                <span class="h-1.5 w-1.5 rounded-full bg-[#c00000]/50"></span>
                            </div>
                            <h3 class="mb-4 text-xl font-bold text-[#0d0e10]">{q}</h3>
                            <p class="leading-relaxed text-[#333] text-sm">{body}</p>
                        </div>
                    ))}
                </div>

                <!-- Cross-system divider -->
                <div class="flex items-center gap-4 my-6">
                    <div class="flex-1 border-t border-dotted border-[#0d0e10]/20"></div>
                    <span class="font-mono text-xs uppercase tracking-[0.2em] text-[#333]">And the question nobody asks until it's too late</span>
                    <div class="flex-1 border-t border-dotted border-[#0d0e10]/20"></div>
                </div>

                <!-- Full-width cross-system card -->
                <div class="border border-[#e5e7eb] rounded-[22px] p-8 bg-white transition-all hover:border-[#0d0e10]/30">
                    <div class="mb-4 flex items-center justify-between">
                        <span class="font-mono text-xs uppercase tracking-widest text-[#333]">Cross-System Governance</span>
                        <span class="h-1.5 w-1.5 rounded-full bg-[#c00000]/50"></span>
                    </div>
                    <h3 class="mb-4 text-xl font-bold text-[#0d0e10]">Who governs the chain?</h3>
                    <p class="leading-relaxed text-[#333] text-sm max-w-3xl">An agent raises a price in Salesforce, triggers a payment in Stripe, and updates inventory in SAP. Each vendor logs their own system. Nobody logs the complete workflow. When the chain breaks, you have fragmented logs across ten consoles — and zero cross-system governance.</p>
                </div>

                <div class="mt-12 px-12 py-24 text-center">
                    <p class="font-mono text-xl md:text-2xl lg:text-3xl font-normal uppercase tracking-[0.05em] text-[#0d0e10] mx-auto leading-tight">
                        Resilient organizations don't just detect these failures.<br />They architect systems that prevent them.
                    </p>
                </div>
            </div>
        </section>

        <!-- ══════════ THE INSIGHT ══════════ -->
        <section class="relative border-t border-dotted border-[#0d0e10]/20 py-24 md:py-32 bg-[#1c1e24]">
            <div class="relative max-w-7xl mx-auto px-6">
                <div class="mb-12">
                    <div class="flex items-center gap-3 mb-6">
                        <span class="h-1.5 w-1.5 rounded-full bg-white/30 shrink-0"></span>
                        <span class="font-mono text-xs uppercase tracking-[0.2em] bg-[#dae4e5] text-[#0d0e10] px-1.5 py-0.5">↳ The insight</span>
                    </div>
                </div>

                <h2 class="max-w-4xl text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight uppercase mb-16" style="background: linear-gradient(120deg, #dae4e5 0%, #2563eb 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
                    Governance cannot live<br />inside the AI.
                </h2>

                <div class="grid md:grid-cols-3 gap-px bg-white/10">
                    <div class="bg-[#1c1e24] px-8 py-8">
                        <p class="font-mono text-xs uppercase tracking-widest text-white/40 mb-4">The Status Quo</p>
                        <p class="text-sm text-white/70 leading-relaxed mb-3"><span class="font-semibold text-white">Control is applied at the endpoint</span> — System Prompts, SDKs, application logic.</p>
                        <p class="text-sm text-white/70 leading-relaxed mb-3"><span class="font-semibold text-white">Safeguards live inside</span> the agent's own code.</p>
                        <p class="text-sm text-white/70 leading-relaxed"><span class="font-semibold text-white">Security is treated as a feature,</span> not a foundation.</p>
                    </div>
                    <div class="bg-[#1c1e24] px-8 py-8">
                        <p class="font-mono text-xs uppercase tracking-widest text-white/40 mb-4">The Structural Flaw</p>
                        <p class="text-sm text-white/70 leading-relaxed mb-3"><span class="font-semibold text-white">Capable agents can bypass</span> their own rules and rewrite their own logs.</p>
                        <p class="text-sm text-white/70 leading-relaxed"><span class="font-semibold text-white">Human oversight does not scale</span> to machine speed.</p>
                    </div>
                    <div class="bg-[#1c1e24] px-8 py-8">
                        <p class="font-mono text-xs uppercase tracking-widest text-white/40 mb-4">The Necessity</p>
                        <p class="text-sm text-white/70 leading-relaxed mb-3"><span class="font-semibold text-white">Governance must sit outside.</span> Security inside the agent is suggestion, not enforcement.</p>
                        <p class="text-sm text-white/70 leading-relaxed"><span class="font-semibold text-white">If the agent can touch the lock,</span> it can pick it.</p>
                    </div>
                </div>

                <div class="mt-16 border-t border-white/10 pt-16 text-center">
                    <p class="text-lg md:text-xl font-semibold text-white/80 leading-relaxed">
                        The only governance that holds is the kind
                        <span class="underline decoration-white/40 ml-1">the agent cannot touch.</span>
                    </p>
                </div>
            </div>
        </section>



        <!-- ══════════ PRODUCT ══════════ -->
        <section class="relative border-t border-dotted border-[#0d0e10]/20 py-24 md:py-32">
            <div class="max-w-7xl mx-auto px-6">

                <!-- Intro text -->
                <div class="mb-12">
                    <div class="flex items-center gap-3 mb-3">
                        <span class="h-1.5 w-1.5 rounded-full bg-[#0d0e10]/20 shrink-0"></span>
                        <span class="font-mono text-xs uppercase tracking-[0.2em] bg-[#fcb830] text-[#0d0e10] px-1.5 py-0.5">↳ Outside the agent. Cannot be overridden.</span>
                    </div>
                    <div class="h-px w-full bg-white/15"></div>
                </div>

                <div class="mb-16 space-y-6">
                    <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight text-[#0d0e10] uppercase">
                        THE ZERO-TRUST<br />AI STACK.
                    </h2>
                    <p class="max-w-2xl text-[#333] leading-relaxed">
                        Kyde is the <span class="font-semibold text-[#0d0e10]">zero-trust governance proxy</span> that sits between your agents and any LLM provider. It intercepts every call, enforces every policy, and signs every entry — before anything reaches the model.
                    </p>
                    <p class="max-w-2xl text-[#333] leading-relaxed">
                        One environment variable. No code changes. Any agent, any provider, under control from day one.
                    </p>
                </div>

                <!-- Architecture Diagram -->
                <div class="mb-16">
                    <ArchitectureDiagram />
                </div>

                <!-- Features intro -->
                <div class="max-w-3xl mx-auto text-center mb-16">
                    <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight text-[#0d0e10] uppercase">
                        Three guarantees. One stack.
                    </h2>
                    <p class="mt-4 text-lg text-[#333]">
                        Not a roadmap. A complete infrastructure layer — deployed in minutes, covering every agent from day one.
                    </p>
                </div>

                <div class="mt-16 grid gap-8 lg:grid-cols-3">
                    <!-- SCOPE -->
                    <div class="border border-[#e5e7eb] rounded-[22px] p-8 bg-white transition-all hover:border-[#0d0e10]/30">
                        <div class="mb-6">
                            <span class="font-mono text-xs text-[#666]">01</span>
                        </div>
                        <h3 class="mb-1 font-mono text-2xl font-bold text-[#0d0e10]">SCOPE</h3>
                        <p class="mb-2 text-sm font-medium text-[#333]">No agent acts without ID & clearance.</p>
                        <p class="mb-6 text-sm leading-relaxed text-[#333]">
                            Every action is checked against policy before execution. Scope enforced at the proxy — not by trust.
                        </p>
                        <ul class="space-y-4">
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">Agent Identity & Behavioral Scope</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">Every agent gets a cryptographic identity and a defined role. No shared service accounts. No anonymous calls.</p>
                            </li>
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">Token & Cost Budgets</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">Hard limits per agent, per role, per day. Enforced before the call is made — not flagged after the bill arrives.</p>
                            </li>
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">API Allowlists & Circuit Breakers</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">Define which endpoints each role can reach. Hard blocks before actions reach external systems.</p>
                            </li>
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">Violation Alerts & Human-in-the-Loop</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">When an agent exceeds its scope — blocked instantly at the proxy. Alerts fire via Slack, Teams, or directly into the Kyde Triage Dashboard. Supervisors review full context, override, or terminate. No silent failures.</p>
                            </li>
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">Zero-Friction Deployment</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">One environment variable or Group Policy push. No code changes. No SDK. Under 100ms latency.</p>
                            </li>
                        </ul>
                    </div>

                    <!-- TRACE -->
                    <div class="border border-[#e5e7eb] rounded-[22px] p-8 bg-white transition-all hover:border-[#0d0e10]/30">
                        <div class="mb-6">
                            <span class="font-mono text-xs text-[#666]">02</span>
                        </div>
                        <h3 class="mb-1 font-mono text-2xl font-bold text-[#0d0e10]">TRACE</h3>
                        <p class="mb-2 text-sm font-medium text-[#333]">No incident without accountability.</p>
                        <p class="mb-6 text-sm leading-relaxed text-[#333]">
                            Every incident traced to a single agent identity. Full chain reconstruction: source, path, destination.
                        </p>
                        <ul class="space-y-4">
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">Traceable Identity</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">Every action traced to a specific agent — not a shared service account. Who acted is always answerable.</p>
                            </li>
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">Causal Context</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">Not just what happened, but why — capturing the last 5 messages before every tool call. Full decision chain reconstruction.</p>
                            </li>
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">Anomaly Detection</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">Behavioral baselines per agent role. Real-time alerts when an agent deviates from expected patterns.</p>
                            </li>
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">SOC Triage Dashboard</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">All behavioral anomalies and policy violations in one place — with full causal context. Built for SOC analysts who need to investigate and respond, not just get paged.</p>
                            </li>
                        </ul>
                    </div>

                    <!-- SEAL -->
                    <div class="border border-[#e5e7eb] rounded-[22px] p-8 bg-white transition-all hover:border-[#0d0e10]/30">
                        <div class="mb-6">
                            <span class="font-mono text-xs text-[#666]">03</span>
                        </div>
                        <h3 class="mb-1 font-mono text-2xl font-bold text-[#0d0e10]">SEAL</h3>
                        <p class="mb-2 text-sm font-medium text-[#333]">Cryptographic audit trail. Court-ready.</p>
                        <p class="mb-6 text-sm leading-relaxed text-[#333]">
                            Every entry signed. Tamper with any record — every subsequent link breaks. Independent of your LLM provider.
                        </p>
                        <ul class="space-y-4">
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">Ed25519-Signed Append-Only Ledger</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">Every entry cryptographically signed at the point of capture. Not a log. A tamper-evident chain of evidence.</p>
                            </li>
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">Provider-Independent Record</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">Your audit trail lives outside Anthropic's, OpenAI's, or any vendor's infrastructure. Yours to export, verify, and submit.</p>
                            </li>
                            <li>
                                <h4 class="text-xs font-mono uppercase tracking-[0.1em] text-[#0d0e10]">Hardware Isolation — TPM/HSM</h4>
                                <p class="mt-0.5 text-sm leading-relaxed text-[#333]">For maximum integrity: signing keys isolated in hardware, never exposed to the host OS. Military-grade for environments where software-only isn't enough.</p>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </section>

        <!-- ══════════ URGENCY ══════════ -->
        <section class="relative border-t border-dotted border-[#0d0e10]/20 py-24 md:py-32">
            <div class="relative max-w-7xl mx-auto px-6">

                <div class="mb-12">
                    <div class="flex items-center gap-3 mb-3">
                        <span class="h-1.5 w-1.5 rounded-full bg-[#0d0e10]/20 shrink-0"></span>
                        <span class="font-mono text-xs uppercase tracking-[0.2em] bg-[#dae4e5] text-[#0d0e10] px-1.5 py-0.5">↳ The legal reality</span>
                    </div>
                    <div class="h-px w-full bg-white/15"></div>
                </div>

                <div class="grid md:grid-cols-2 gap-16 items-start mb-16">
                    <div>
                        <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight text-[#0d0e10] uppercase mb-6">
                            Your agents act on your behalf.<br />Courts agree.
                        </h2>
                        <p class="text-[#333] leading-relaxed mb-6">
                            They negotiate, transact, and make commitments in your name. Regulators, auditors, and courts will hold you accountable for what they do — and the obligations are already in force.
                        </p>
                        <div class="border-t border-b border-dotted border-[#0d0e10]/20 p-6 mb-6">
                            <p class="font-mono text-xs uppercase tracking-widest text-[#333] mb-3">Moffatt v. Air Canada</p>
                            <p class="font-mono text-sm text-[#333] leading-relaxed border-l-2 border-dotted border-[#0d0e10]/25 pl-4">
                                "Air Canada cannot avoid responsibility for information provided by its agent."
                            </p>
                        </div>
                        <p class="text-sm text-[#666] leading-relaxed">
                            The question isn't whether you need an evidence trail. It's whether yours can withstand scrutiny.
                        </p>
                    </div>

                    <div class="grid gap-4 grid-cols-2">
                        {[
                            { name: "NIS-2",     desc: "~160,000 companies in DE/EU. Critical + important sectors. AI systems included.", deadline: "In force · DE: NIS2UmsuCG", urgent: true },
                            { name: "DORA",      desc: "Financial entity resilience",  deadline: "In force: Jan 2025",  urgent: false },
                            { name: "EU AI Act", desc: "High-risk AI system audit trail & logging requirements", deadline: "Phasing in 2026/2027", urgent: false },
                            { name: "GDPR 35",   desc: "Automated processing DPIA",    deadline: "In force",            urgent: false },
                        ].map(({ name, desc, deadline, urgent }) => (
                            <div class={`border rounded-[14px] p-5 ${urgent ? "border-[#c00000]/30 bg-[#fff8f8]" : "border-[#dae4e5] bg-[#dae4e5]/20"}`}>
                                <div class="flex items-start justify-between mb-2">
                                    <h3 class="font-mono text-sm font-bold text-[#0d0e10]">{name}</h3>
                                    {urgent && <span class="font-mono text-[9px] border border-dotted border-[#c00000]/50 px-1.5 py-0.5 text-[#c00000]">Urgent</span>}
                                </div>
                                <p class="text-xs text-[#555] mb-2">{desc}</p>
                                <p class={`font-mono text-[10px] ${deadline.includes('In force') ? 'text-[#0a7c4e]' : 'text-[#333]'}`}>{deadline}</p>
                            </div>
                        ))}
                    </div>
                </div>
            </div>
        </section>

        <!-- ══════════ CLOSING CTA ══════════ -->
        <section class="relative border-t border-dotted border-[#0d0e10]/20 py-24 md:py-32">
            <div class="relative max-w-7xl mx-auto px-6">
                <div class="mx-auto max-w-3xl text-center">
                    <p class="mb-3 font-mono text-xs uppercase tracking-[0.2em] bg-[#dae4e5] text-[#0d0e10] px-1.5 py-0.5 w-fit mx-auto">↳ Get started</p>
                    <div class="mb-12 h-px bg-[#0d0e10]/15"></div>
                    <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight text-[#0d0e10] mb-4">
                        Your agents are a workforce.
                    </h2>
                    <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight text-[#333] mb-12">
                        Your workforce is critical infrastructure.<br />Govern accordingly.
                    </h2>
                    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
                        <button
                            onclick="openModal()"
                            class="inline-flex items-center gap-2 bg-[#0d0e10] rounded-full px-8 py-4 text-xs font-mono uppercase tracking-[0.1em] text-[#f1f3f5] hover:bg-[#333] transition-all"
                        >
                            Book Demo →
                        </button>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <Footer />
</Base>
