---
import Base from '../layouts/Base.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
---

<Base
  title="Pricing — KYDE"
  description="One platform. Three levels of control. Every organization starts with full visibility. Governance and enforcement activate as your fleet scales."
>
  <Navbar />

  <main>

    <!-- ══════════ HERO ══════════ -->
    <section class="relative overflow-hidden pt-36 pb-24 md:pt-48 md:pb-36">
      <div class="bg-grid absolute inset-0 pointer-events-none"></div>
      <div class="relative max-w-7xl mx-auto px-6">

        <div class="mb-10">
          <div class="flex items-center gap-3 mb-3">
            <span class="h-1.5 w-1.5 rounded-full bg-zinc-500 shrink-0"></span>
            <span class="font-mono text-xs uppercase tracking-[0.2em] text-[#333]">↳ Pricing</span>
          </div>
          <div class="h-px w-full bg-[#e8e9eb]"></div>
        </div>

        <h1 class="hero-h1 max-w-5xl text-5xl text-[#0d0e10] uppercase tracking-tight md:text-6xl lg:text-7xl">
          One platform.<br />
          <span class="gradient-text">Three levels of control.</span>
        </h1>

        <div class="mt-16 border-t border-dotted border-[#0d0e10]/25 pt-10 grid md:grid-cols-[200px_1fr] gap-8 items-start">
          <div>
            <p class="text-xs font-bold uppercase tracking-[0.15em] text-[#0d0e10]">Structure</p>
            <p class="mt-2 font-mono text-xs text-[#666]">[MODULAR]</p>
          </div>
          <div>
            <p class="text-sm uppercase tracking-wide leading-relaxed text-[#333]">
              Every organization starts with full visibility. Governance and enforcement activate as your fleet scales.
            </p>
          </div>
        </div>

      </div>
    </section>

    <!-- ══════════ TIERS ══════════ -->
    <section class="border-t border-dotted border-[#0d0e10]/25 py-16 md:py-24">
      <div class="max-w-7xl mx-auto px-6">
        <div class="grid gap-6 lg:grid-cols-3">

          <!-- PHASE 1 — OBSERVE -->
          <div class="border-t border-b border-dotted border-[#0d0e10]/25 transition-all flex flex-col">
            <div class="px-8 py-8 border-b border-dotted border-[#0d0e10]/25">
              <p class="font-mono text-xs uppercase tracking-[0.2em] text-[#333] mb-3">Phase 1</p>
              <h2 class="text-2xl font-bold text-[#0d0e10] uppercase tracking-tight mb-1">Observe</h2>
              <p class="text-sm text-[#333] uppercase tracking-widest">The evidence layer.</p>
            </div>
            <div class="px-8 py-8 flex-1">
              <p class="text-sm leading-relaxed text-[#333] mb-8">
                The foundation. Deploy in minutes. Every agent action signed, chained, and preserved — before you need it.
              </p>
              <ul class="space-y-3">
                {[
                  "Tamper-evident append-only ledger",
                  "Ed25519 signing + SHA-256 hash chaining",
                  "Agent identity registration",
                  "Causal context capture (last 5 messages per tool call)",
                  "Provider-agnostic interception — every model, every provider",
                  "CLI audit and verification tools",
                  "One environment variable deployment",
                ].map((item) => (
                  <li class="flex gap-3 text-sm text-[#333] leading-relaxed">
                    <span class="font-mono text-[#333] shrink-0 mt-0.5">→</span>
                    <span>{item}</span>
                  </li>
                ))}
              </ul>
            </div>
            <div class="px-8 py-6 border-t border-dotted border-[#0d0e10]/25">
              <button
                onclick="openModal()"
                class="w-full inline-flex items-center justify-center gap-2 bg-[#0d0e10] px-6 py-3 text-sm font-semibold text-[#f1f3f5] hover:bg-[#333] transition-all"
              >
                Book a Demo →
              </button>
            </div>
          </div>

          <!-- PHASE 2 — GOVERN -->
          <div class="border-t border-b border-dotted border-[#0d0e10]/25 transition-all flex flex-col relative">
            <div class="absolute top-0 left-0 right-0 h-px bg-amber-400/40"></div>
            <div class="px-8 py-8 border-b border-dotted border-[#0d0e10]/25">
              <p class="font-mono text-xs uppercase tracking-[0.2em] text-[#333] mb-3">Phase 2</p>
              <h2 class="text-2xl font-bold text-[#0d0e10] uppercase tracking-tight mb-1">Govern</h2>
              <p class="text-sm text-[#333] uppercase tracking-widest">The control layer.</p>
            </div>
            <div class="px-8 py-8 flex-1">
              <p class="text-sm leading-relaxed text-[#333] mb-4">
                Active fleet management. Role-based identity, resource allocation, and policy enforcement — across your entire agent workforce.
              </p>
              <p class="text-xs font-mono uppercase tracking-widest text-[#666] mb-4">Everything in Observe, plus:</p>
              <ul class="space-y-3">
                {[
                  { text: "Role-based identity and priority assignment", soon: false },
                  { text: "Token and cost budgets per agent, per role, per team", soon: false },
                  { text: "API allowlists and blocklists per role", soon: false },
                  { text: "Behavioral anomaly detection and out-of-policy alerting", soon: true },
                  { text: "Multi-user RBAC with SSO / Okta / Azure AD", soon: true },
                  { text: "Continuous data integrity verification", soon: false },
                ].map(({ text, soon }) => (
                  <li class="flex gap-3 text-sm text-[#333] leading-relaxed">
                    <span class="font-mono text-[#333] shrink-0 mt-0.5">→</span>
                    <span>{text}{soon && <span class="ml-2 font-mono text-xs text-[#4472c4]">Coming Soon</span>}</span>
                  </li>
                ))}
              </ul>
            </div>
            <div class="px-8 py-6 border-t border-dotted border-[#0d0e10]/25">
              <button
                onclick="openModal()"
                class="w-full inline-flex items-center justify-center gap-2 bg-[#0d0e10] px-6 py-3 text-sm font-semibold text-[#f1f3f5] hover:bg-[#333] transition-all"
              >
                Book a Demo →
              </button>
            </div>
          </div>

          <!-- ENTERPRISE -->
          <div class="border-t border-b border-dotted border-[#0d0e10]/25 transition-all flex flex-col">
            <div class="px-8 py-8 border-b border-dotted border-[#0d0e10]/25">
              <p class="font-mono text-xs uppercase tracking-[0.2em] text-[#333] mb-3">Enterprise</p>
              <h2 class="text-2xl font-bold text-[#0d0e10] uppercase tracking-tight mb-1">Resilience</h2>
              <p class="text-sm text-[#333] uppercase tracking-widest">The resilience layer.</p>
            </div>
            <div class="px-8 py-8 flex-1">
              <p class="text-sm leading-relaxed text-[#333] mb-4">
                For regulated environments where software-only governance isn't enough. Hardware-rooted attestation, machine-readable policy, and agent-to-agent trust delegation.
              </p>
              <p class="text-xs font-mono uppercase tracking-widest text-[#666] mb-4">Everything in Govern, plus:</p>
              <ul class="space-y-3">
                {[
                  { text: "HSM / PKCS#11 hardware-rooted signing", tag: "Roadmap" },
                  { text: "TEE attestation (TDX/SEV-SNP)", tag: "Roadmap" },
                  { text: "Policy Enforcement Point with hard circuit breakers", tag: "Roadmap" },
                  { text: "Machine-readable Proof of Authority", tag: "Roadmap" },
                  { text: "Agent-to-agent trust delegation", tag: "Roadmap" },
                  { text: "Dedicated support SLA", tag: null },
                  { text: "EU data residency options", tag: null },
                  { text: "ISO 27001 / BSI procurement track", tag: "In progress" },
                ].map(({ text, tag }) => (
                  <li class="flex gap-3 text-sm text-[#333] leading-relaxed">
                    <span class="font-mono text-[#333] shrink-0 mt-0.5">→</span>
                    <span>{text}{tag && <span class={`ml-2 font-mono text-xs ${tag === 'Roadmap' ? 'text-[#4472c4]' : tag === 'In progress' ? 'text-[#333]' : 'text-[#666]'}`}>{tag}</span>}</span>
                  </li>
                ))}
              </ul>
            </div>
            <div class="px-8 py-6 border-t border-dotted border-[#0d0e10]/25">
              <a
                href="mailto:hello@kyde.com"
                class="w-full inline-flex items-center justify-center gap-2 border border-dotted border-[#0d0e10]/20 px-6 py-3 text-sm font-semibold text-[#333] hover:border-[#0d0e10] hover:text-[#0d0e10] transition-all"
              >
                Contact Sales →
              </a>
            </div>
          </div>

        </div>
      </div>
    </section>

    <!-- ══════════ ALL PLANS ══════════ -->
    <section class="border-t border-dotted border-[#0d0e10]/25 py-16 md:py-24">
      <div class="max-w-7xl mx-auto px-6">

        <div class="mb-10">
          <p class="font-mono text-xs uppercase tracking-[0.2em] text-[#333] mb-3">↳ All Plans Include</p>
          <div class="h-px bg-[#0d0e10]/40"></div>
        </div>

        <div class="grid border-t border-b border-dotted border-[#0d0e10]/25 divide-y sm:divide-y-0 sm:divide-x divide-dotted divide-[#0d0e10]/15 sm:grid-cols-2 lg:grid-cols-5">
          {[
            { label: "Provider coverage", value: "Every model. Every provider." },
            { label: "Deployment", value: "One ENV variable or Group Policy push" },
            { label: "Latency overhead", value: "<100ms" },
            { label: "Code changes required", value: "None" },
            { label: "Data ownership", value: "Yours — not ours, not your vendor's" },
          ].map(({ label, value }) => (
            <div class="bg-[#0d0e10] px-6 py-6">
              <p class="font-mono text-xs uppercase tracking-widest text-[#333] mb-2">{label}</p>
              <p class="text-sm text-[#333] leading-relaxed">{value}</p>
            </div>
          ))}
        </div>

      </div>
    </section>

    <!-- ══════════ CLOSING CTA ══════════ -->
    <section class="border-t border-dotted border-[#0d0e10]/25 py-24 md:py-32">
      <div class="max-w-7xl mx-auto px-6">
        <div class="border border-dotted border-[#0d0e10]/20 p-12 md:p-20">
          <div class="max-w-3xl">
            <p class="mb-3 font-mono text-xs uppercase tracking-[0.2em] text-[#333]">↳ Not sure which tier fits?</p>
            <div class="mb-8 h-px bg-[#0d0e10]/40"></div>
            <h2 class="text-3xl md:text-4xl font-bold tracking-tight text-[#0d0e10] mb-4">
              Most enterprises start with Observe and activate Govern within 90 days.
            </h2>
            <p class="text-lg text-[#333] mb-10">
              Book a 30-minute call — we'll tell you exactly where you are and what you need.
            </p>
            <button
              onclick="openModal()"
              class="inline-flex items-center gap-2 bg-[#0d0e10] px-6 py-3.5 text-sm font-semibold text-[#f1f3f5] hover:bg-[#333] transition-all"
            >
              Book a Demo →
            </button>
          </div>
        </div>
      </div>
    </section>

  </main>

  <Footer />
</Base>
