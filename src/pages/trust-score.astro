---
import Base from '../layouts/Base.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

type ScoreEvent = { label: string; ok: boolean };
const scoreEvents: ScoreEvent[] = [
  { label: "Identity verified (Ed25519)",                           ok: true  },
  { label: "Operating within approved scope",                       ok: true  },
  { label: "One unauthorized export attempt intercepted and blocked", ok: false },
];

type MandateLine = { label: string; value: string; cls: string };
const mandateExample: MandateLine[] = [
  { label: "Declared role", value: "Customer Support",                        cls: "text-ink-1"    },
  { label: "Permitted",     value: "Read tickets · Send customer messages",   cls: "text-live" },
  { label: "Observed",      value: "Export all contacts",                     cls: "text-alert" },
  { label: "Result",        value: "Mandate deviation · Action blocked · Trust Score updated", cls: "text-ink-1" },
];

type PeerRow = { name: string; score: number; cls: string; note?: string };
const benchmarkExample: PeerRow[] = [
  { name: "this agent",        score: 84, cls: "text-live", note: "above median" },
  { name: "peer median",       score: 78, cls: "text-ink-0" },
  { name: "peer p10",          score: 61, cls: "text-warn" },
  { name: "flagged threshold", score: 40, cls: "text-alert" },
];

type Dimension = { label: string; question: string; desc: string; hasExample: boolean };
const dimensions: Dimension[] = [
  {
    label:      "Identity",
    question:   "Who performed the action?",
    desc:       "Each agent receives a cryptographically verifiable identity. Self-hosted agents are identified per instance. Cloud-executed agents are bound to enterprise-issued credentials and connector authorizations.",
    hasExample: false,
  },
  {
    label:      "Mandate",
    question:   "Was the action consistent with the agent's declared role?",
    desc:       "Every agent operates within a defined mandate. Observed actions are continuously compared against that mandate.",
    hasExample: true,
  },
  {
    label:      "Baseline",
    question:   "Is the behavior consistent with this agent's established pattern?",
    desc:       "Normal operating characteristics are learned over time, including call sequences, timing, endpoint usage and execution profiles. Persistent deviation may indicate compromise, misuse or unintended behavior.",
    hasExample: false,
  },
  {
    label:      "Benchmark",
    question:   "How does this agent compare with similar agents?",
    desc:       "Anonymized comparison across comparable role classes provides additional context. A CRM agent is evaluated against CRM agents. A coding agent is evaluated against coding agents. The comparison reflects behavior rather than implementation.",
    hasExample: false,
  },
];
---

<Base
  title="KYDE Trust Score™, Independent Measurement for AI Agents"
  description="The KYDE Trust Score is an independent behavioral measurement for AI agents, computed from four observable signals at the execution boundary. Always published with its Coverage figure."
>
  <Navbar />
  <main>

    <!-- ══════════ HERO ══════════ -->
    <section class="relative bg-bg-1 overflow-hidden min-h-screen flex flex-col justify-center pt-24 pb-20">
      <div class="absolute inset-0 bg-grid opacity-[0.06] pointer-events-none"></div>
      <div class="relative max-w-7xl mx-auto px-6 w-full">
        <div class="grid lg:grid-cols-2 gap-16 lg:gap-0 lg:divide-x lg:divide-line-0 [&>*:first-child]:lg:pr-12 [&>*:last-child]:lg:pl-12 items-center">

          <!-- Copy -->
          <div>
            <div class="flex items-center gap-2 mb-8">
              <span class="font-mono text-[10px] uppercase tracking-widest text-white/30 border border-white/15 px-3 py-1">Product</span>
              <span class="font-mono text-[10px] text-white/20">·</span>
              <span class="font-mono text-[10px] uppercase tracking-widest text-gold/90 border border-gold/30 px-3 py-1">Trust Score™</span>
            </div>
            <h1 class="hero-h1 text-4xl md:text-5xl lg:text-6xl text-white uppercase tracking-tight leading-[1.05] mb-6">
              Trust begins with independent measurement.
            </h1>
            <p class="text-lg text-white/55 leading-relaxed max-w-lg mb-4">
              AI agents increasingly perform actions on behalf of organizations. Trust requires those actions to be observable, attributable and measurable.
            </p>
            <p class="text-base text-white/35 leading-relaxed max-w-lg mb-4">
              KYDE computes an independent Trust Score from the observable behavior of AI agents at the execution boundary.
            </p>
            <p class="text-base text-white/30 leading-relaxed max-w-lg mb-10">
              Deploy through a single gateway. Existing agents continue to operate unchanged.
            </p>
            <div class="flex flex-col sm:flex-row gap-3">
              <!-- Starter CTA intentionally disabled here — page CTAs stay Talk to Us (SBX-001 gate itself is lifted, navbar has Start Free)
              <a href="/starter" class="inline-flex items-center gap-2 bg-white text-cta-ink px-6 py-3.5 text-xs font-mono uppercase tracking-[0.12em] hover:bg-cta-hover transition-all">
                Deploy the Gateway →
              </a>
              -->
              <button
                onclick="openModal()"
                class="inline-flex items-center gap-2 bg-white text-cta-ink px-6 py-3.5 text-xs font-mono uppercase tracking-[0.12em] hover:bg-cta-hover transition-all"
              >
                Talk to Us →
              </button>
              <a
                href="/blog/confidence-infrastructure-part-3"
                class="inline-flex items-center gap-2 border border-white/25 text-white/60 px-6 py-3.5 text-xs font-mono uppercase tracking-[0.12em] hover:border-white/60 hover:text-white/90 transition-all"
              >
                Technical Architecture →
              </a>
            </div>
          </div>

          <!-- Score mockup -->
          <div class="border border-white/10 bg-white/[0.03] p-8 md:p-10">
            <div class="mb-6 pb-5 border-b border-white/8">
              <p class="font-mono text-[9px] uppercase tracking-[0.2em] text-white/30 mb-1">KYDE Trust Score™</p>
              <p class="font-mono text-[10px] text-white/20">CRM Sales Agent · Acme Corp</p>
            </div>

            <div class="flex items-end gap-4 mb-3">
              <p id="ts-score" class="font-mono text-8xl font-bold text-white leading-none tracking-tighter">84</p>
              <p class="font-mono text-sm text-white/20 pb-2">/100</p>
            </div>
            <p class="text-sm text-white/35 mb-8 leading-snug row-in" style="animation-delay:1.1s">Within the expected range for comparable enterprise agents.</p>

            <div class="space-y-3.5 mb-8">
              {scoreEvents.map(({ label, ok }, i) => (
                <div class="flex items-start gap-3 row-in" style={`animation-delay:${1.3 + i * 0.25}s`}>
                  <span class={`font-mono text-base leading-none shrink-0 mt-0.5 ${ok ? 'text-live' : 'text-alert'}`}>{ok ? '✓' : '⚠'}</span>
                  <span class={`text-sm leading-snug ${ok ? 'text-white/45' : 'text-white/65'}`}>{label}</span>
                </div>
              ))}
            </div>

            <div class="border-t border-white/8 pt-5 row-in" style="animation-delay:2.1s">
              <div class="flex items-center justify-between mb-2">
                <p class="font-mono text-[9px] uppercase tracking-[0.2em] text-white/25">Coverage</p>
                <p id="ts-coverage" class="font-mono text-xl font-bold text-live">92%</p>
              </div>
              <p class="text-xs text-white/20 leading-snug">92% of observable agent activity contributed to the score.</p>
            </div>
          </div>

        </div>
      </div>
    </section>

    <script>
      // Score + coverage count up on load, mono-stable via easing
      const rmTs = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
      const scoreEl = document.getElementById("ts-score");
      const covEl = document.getElementById("ts-coverage");
      if (scoreEl && covEl && !rmTs) {
        const T = 1400;
        const start = performance.now();
        const ease = (x: number) => 1 - Math.pow(1 - x, 3);
        function tick(now: number) {
          const p = Math.min((now - start) / T, 1);
          const e = ease(p);
          scoreEl!.textContent = String(Math.round(84 * e));
          covEl!.textContent = `${Math.round(92 * e)}%`;
          if (p < 1) requestAnimationFrame(tick);
        }
        requestAnimationFrame(tick);
      }
    </script>

    <!-- ══════════ INDEPENDENT MEASUREMENT ══════════ -->
    <section class="border-t border-line-0 bg-bg-1">
      <div class="max-w-7xl mx-auto px-6 py-16 md:py-24 md:border-x md:border-line-0">
        <div class="grid md:grid-cols-2 gap-12 md:gap-0 md:divide-x md:divide-line-0 [&>*:first-child]:md:pr-12 [&>*:last-child]:md:pl-12 items-start">
          <div>
            <p class="font-mono text-xs uppercase tracking-widest text-ink-2 mb-6">Independent measurement</p>
            <h2 class="text-3xl md:text-4xl font-bold text-ink-0 leading-tight">
              Organizations increasingly rely on AI agents across models, providers and enterprise systems.
            </h2>
          </div>
          <div class="md:pt-10">
            <p class="text-base text-ink-1 leading-relaxed mb-5">
              Existing governance tools describe individual environments. They cannot provide a consistent measure of behavior across organizational boundaries.
            </p>
            <p class="text-base text-ink-1 leading-relaxed">
              The Trust Score addresses this by measuring observable actions where they occur: at the execution boundary.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- ══════════ COVERAGE ══════════ -->
    <section class="border-t border-line-0 bg-bg-0">
      <div class="max-w-7xl mx-auto px-6 py-16 md:py-24 md:border-x md:border-line-0">

        <div class="mb-12">
          <div class="flex items-end justify-between border-b border-line-0 pb-3"><span class="font-mono text-[11px] uppercase tracking-[0.2em] text-ink-1">01 · Coverage</span></div>
        </div>

        <div class="grid md:grid-cols-2 gap-12 md:gap-0 md:divide-x md:divide-line-0 [&>*:first-child]:md:pr-12 [&>*:last-child]:md:pl-12 items-start mb-16">
          <div>
            <h2 class="text-3xl md:text-4xl font-bold text-ink-0 leading-tight mb-6">
              Every score requires a denominator.
            </h2>
            <p class="text-base text-ink-1 leading-relaxed mb-4">
              A Trust Score without Coverage describes only part of the observed reality.
            </p>
            <p class="text-base text-ink-1 leading-relaxed mb-4">
              Coverage expresses how much of an agent's observable activity passed through governed paths and therefore contributed to the score.
            </p>
            <p class="text-base text-ink-1 leading-relaxed">
              Coverage of 92% and Coverage of 41% may produce the same score, but they describe fundamentally different levels of confidence. For this reason, every Trust Score is accompanied by a Coverage Report.
            </p>
          </div>
          <div class="border border-line-0 bg-bg-1 p-6 md:p-8">
            <p class="font-mono text-[10px] uppercase tracking-widest text-ink-2 mb-6">Coverage Report · Acme Corp</p>
            <div class="space-y-6">
              {[
                { label: "Governed",    pct: 92, desc: "Observed, measured and cryptographically signed.",                                                                          color: "var(--color-live)" },
                { label: "Governable",  pct:  6, desc: "Known paths that have not yet been routed through the gateway.",                                                           color: "var(--color-warn)" },
                { label: "Unobserved",  pct:  2, desc: "Provider-internal execution outside enterprise visibility, such as internal reasoning, delegated sub-agents or provider-native integrations.", color: "var(--color-ink-2)" },
              ].map(({ label, pct, desc, color }) => (
                <div>
                  <div class="flex items-center justify-between mb-1.5">
                    <span class="font-mono text-[10px] uppercase tracking-widest text-ink-1">{label}</span>
                    <span class="font-mono text-sm font-bold" style={`color:${color}`}>{pct}%</span>
                  </div>
                  <div class="h-1 bg-bg-3 rounded-full overflow-hidden mb-2">
                    <div class="h-full rounded-full bar-fill" style={`width:${pct}%;background-color:${color};opacity:0.75`}></div>
                  </div>
                  <p class="text-xs text-ink-2 leading-snug">{desc}</p>
                </div>
              ))}
            </div>
          </div>
        </div>

        <!-- Architecture diagram -->
        <div class="mb-8">
          <p class="font-mono text-xs uppercase tracking-widest text-ink-2 mb-6">The execution boundary</p>
          <div class="grid md:grid-cols-[1fr_36px_1fr_36px_1fr] items-stretch gap-0">
            <div class="border border-line-0 bg-bg-1 p-6">
              <p class="font-mono text-[10px] uppercase tracking-widest text-ink-2 mb-3">Unobserved</p>
              <p class="text-sm font-semibold text-ink-2 mb-3">Provider-internal</p>
              <p class="text-xs text-ink-2 leading-relaxed">Internal reasoning, sub-agent delegation, provider-native connectors. Quantified in the Coverage Report.</p>
            </div>
            <div class="hidden md:flex items-center justify-center text-ink-2 font-mono">→</div>
            <div class="border border-ink-0/60 bg-bg-1 p-6 md:-my-2">
              <p class="font-mono text-[10px] uppercase tracking-widest text-ink-0/60 mb-3">KYDE Gateway</p>
              <p class="text-sm font-semibold text-ink-0 mb-3">Execution boundary</p>
              <p class="text-xs text-ink-1 leading-relaxed">Verifies agent identity, evaluates actions against mandate, computes the Trust Score, signs every event, and enforces organizational policy where required.</p>
            </div>
            <div class="hidden md:flex items-center justify-center text-ink-2 font-mono">→</div>
            <div class="border border-live/25 bg-bg-1 p-6">
              <p class="font-mono text-[10px] uppercase tracking-widest text-live mb-3">Governed</p>
              <p class="text-sm font-semibold text-ink-1 mb-3">Enterprise systems</p>
              <p class="text-xs text-ink-1 leading-relaxed">APIs, databases, MCP servers, SaaS endpoints. Every action measured, signed and recorded.</p>
            </div>
          </div>
        </div>

        <div class="border-l-2 border-line-0 pl-6 max-w-2xl">
          <p class="text-sm text-ink-1 leading-relaxed">The resulting measurement is independent of model provider or framework.</p>
        </div>

      </div>
    </section>

    <!-- ══════════ HOW THE SCORE IS COMPUTED ══════════ -->
    <section class="border-t border-line-0 bg-bg-1">
      <div class="max-w-7xl mx-auto px-6 py-16 md:py-24 md:border-x md:border-line-0">

        <div class="mb-12">
          <div class="flex items-end justify-between border-b border-line-0 pb-3"><span class="font-mono text-[11px] uppercase tracking-[0.2em] text-ink-1">02 · How the Trust Score is computed</span></div>
        </div>

        <div class="grid md:grid-cols-2 gap-12 md:gap-0 md:divide-x md:divide-line-0 [&>*:first-child]:md:pr-12 [&>*:last-child]:md:pl-12 items-center mb-12">
          <div>
            <h2 class="text-3xl md:text-4xl font-bold text-ink-0 leading-tight mb-6">
              The score is derived from four observable signals.
            </h2>
            <p class="text-base text-ink-1 leading-relaxed">
              No access to model internals is required. No provider-supplied assessment is used.
            </p>
          </div>

          <!-- FIG.3 · score computation -->
          <div class="relative flex items-center justify-center">
            <span class="fig-label absolute top-0 right-0">FIG.3</span>
            <svg viewBox="0 0 620 400" class="w-full max-w-lg h-auto" aria-hidden="true">
              <defs>
                <pattern id="cube-dots" width="10" height="10" patternUnits="userSpaceOnUse">
                  <circle cx="2" cy="2" r="1" fill="var(--color-ink-0)" fill-opacity="0.22" />
                </pattern>
              </defs>

              <!-- input chips -->
              {[
                { label: "IDENTITY",  y: 90  },
                { label: "MANDATE",   y: 150 },
                { label: "BASELINE",  y: 210 },
                { label: "BENCHMARK", y: 270 },
              ].map(({ label, y }) => (
                <>
                  <rect x="30" y={y - 13} width="108" height="26" fill="var(--color-bg-3)" stroke="var(--color-ink-0)" stroke-opacity="0.15" stroke-width="1" />
                  <text x="84" y={y + 4} text-anchor="middle" fill="var(--color-ink-0)" fill-opacity="0.65" style="font-family:'JetBrains Mono Variable',monospace;font-size:10px;letter-spacing:0.14em;">{label}</text>
                  <path d={`M138 ${y} L200 ${y} L240 ${180}`} fill="none" stroke="var(--color-ink-0)" stroke-opacity="0.18" stroke-width="1" stroke-dasharray="3 3" />
                </>
              ))}

              <!-- isometric cube -->
              <path class="draw" pathLength="1" d="M330 80 L420 125 L420 235 L330 280 L240 235 L240 125 Z" fill="none" stroke="var(--color-ink-0)" stroke-opacity="0.4" stroke-width="1" />
              <path class="draw" pathLength="1" d="M240 125 L330 170 L420 125" fill="none" stroke="var(--color-ink-0)" stroke-opacity="0.4" stroke-width="1" />
              <path class="draw" pathLength="1" d="M330 170 L330 280" fill="none" stroke="var(--color-ink-0)" stroke-opacity="0.4" stroke-width="1" />
              <path d="M330 80 L420 125 L330 170 L240 125 Z" fill="url(#cube-dots)" />

              <!-- output callout -->
              <path d="M420 180 L490 180" fill="none" stroke="var(--color-ink-0)" stroke-opacity="0.3" stroke-width="1" />
              <circle cx="420" cy="180" r="3" fill="var(--color-ink-0)" />
              <text x="498" y="172" text-anchor="start" fill="var(--color-ink-0)" fill-opacity="0.55" style="font-family:'JetBrains Mono Variable',monospace;font-size:10px;letter-spacing:0.18em;">TRUST SCORE</text>
              <text x="498" y="198" text-anchor="start" fill="var(--color-live)" style="font-family:'JetBrains Mono Variable',monospace;font-size:24px;font-weight:700;">84</text>

              <!-- coverage footnote -->
              <text x="330" y="320" text-anchor="middle" fill="var(--color-ink-0)" fill-opacity="0.3" style="font-family:'JetBrains Mono Variable',monospace;font-size:10px;letter-spacing:0.14em;">COVERAGE 92%</text>
            </svg>
          </div>
        </div>

        <div class="space-y-px">
          {dimensions.map(({ label, question, desc, hasExample }) => (
            <div class="grid md:grid-cols-[200px_1fr] divide-y md:divide-y-0 md:divide-x divide-line-0 border border-line-0 bg-bg-1">
              <div class="p-6 md:p-8 flex flex-col justify-center">
                <p class="text-lg font-bold text-ink-0 mb-1.5">{label}</p>
                <p class="text-sm text-ink-2 leading-snug">{question}</p>
              </div>
              <div class="p-6 md:p-8">
                <p class="text-sm text-ink-1 leading-relaxed">{desc}</p>
                {hasExample && (
                  <div class="mt-5 border border-line-0 bg-bg-2 p-5">
                    <p class="font-mono text-[9px] uppercase tracking-widest text-ink-2 mb-4">Example</p>
                    <div class="space-y-2 font-mono text-xs">
                      {mandateExample.map(({ label: l, value, cls }) => (
                        <div class="grid grid-cols-[100px_1fr] gap-3">
                          <span class="text-ink-2">{l}</span>
                          <span class={cls}>{value}</span>
                        </div>
                      ))}
                    </div>
                  </div>
                )}
                {label === "Benchmark" && (
                  <div class="mt-5 border border-line-0 bg-bg-2 p-5">
                    <p class="font-mono text-[9px] uppercase tracking-widest text-ink-2 mb-4">role class: crm-agents · anonymized · n=214</p>
                    <div class="space-y-2.5 font-mono text-xs">
                      {benchmarkExample.map(({ name, score, cls, note }) => (
                        <div class="flex items-baseline gap-2">
                          <span class="text-ink-2 shrink-0">{name}</span>
                          <span class="flex-1 border-b border-dashed border-line-1 translate-y-[-3px]"></span>
                          <span class={`font-bold ${cls}`}>{score}</span>
                          {note && <span class="text-ink-2 text-[10px]">· {note}</span>}
                        </div>
                      ))}
                    </div>
                  </div>
                )}
              </div>
            </div>
          ))}
        </div>

      </div>
    </section>

    <!-- ══════════ 03 THE TRUST LADDER ══════════ -->
    <!-- The score measures behavior; the ladder says what that behavior entitles
         an agent to. They belong on the same page: one is the measurement, the
         other is the consequence. Levels follow the canonical L0–L5 ladder. -->
    <section id="ladder" class="scroll-mt-20 border-t border-line-0 bg-bg-1">
      <div class="max-w-7xl mx-auto px-6 py-16 md:py-24 md:border-x md:border-line-0">

        <div class="mb-12">
          <div class="flex items-end justify-between border-b border-line-0 pb-3">
            <span class="font-mono text-[11px] uppercase tracking-[0.2em] text-ink-1">03 · The Trust Ladder</span>
            <span class="fig-label">FIG.4</span>
          </div>
        </div>

        <div class="grid md:grid-cols-2 gap-12 md:gap-0 md:divide-x md:divide-line-0 [&>*:first-child]:md:pr-12 [&>*:last-child]:md:pl-12 items-start mb-12">
          <div>
            <h2 class="text-3xl md:text-4xl font-bold text-ink-0 leading-tight mb-6">
              A score on its own grants nothing.
            </h2>
            <p class="text-base text-ink-1 leading-relaxed mb-4">
              The Trust Ladder turns the measurement into a decision. Six levels, L0 to L5, from an assistant that only reads and suggests to an operator running unattended within a defined scope. Each level names the work an agent may be trusted with, and the evidence it has to keep producing to stay there.
            </p>
            <p class="text-base text-ink-1 leading-relaxed">
              Up to L3, observability carries the level. From L4, where money moves or something physical happens, watching is no longer a qualification: the boundary has to be enforced, not just logged. Drift takes a level away as readily as evidence earns it.
            </p>
          </div>

          <div>
            <div class="flex flex-col gap-px">
              {[
                { l: "L0", name: "Assistant",                     work: "Reads, answers, suggests. No actions.",                    band: "Home · sandbox" },
                { l: "L1", name: "Tool User",                     work: "Tool calls, each one attributable.",                       band: "Home · sandbox" },
                { l: "L2", name: "Workflow Executor",             work: "Multi-step work: tickets, workflows.",                     band: "Company · production" },
                { l: "L3", name: "Operational Authority",         work: "Production responsibility, such as deploying code.",       band: "Company · production" },
                { l: "L4", name: "Financial / Physical Authority", work: "Money or physical effect, such as releasing payments.",   band: "Critical infrastructure · NIS-2 / DORA" },
                { l: "L5", name: "Autonomous Operator",           work: "Unattended operation within a defined scope.",             band: "Critical infrastructure · NIS-2 / DORA" },
              ].map(({ l, name, work, band }, i) => (
                <div class={`border p-4 ${i >= 4 ? "border-line-0 bg-ink-0/[0.03]" : "border-line-0"}`}>
                  <div class="flex items-baseline gap-3 mb-1.5">
                    <span class="font-mono text-sm font-bold text-ink-0 shrink-0">{l}</span>
                    <span class="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-0">{name}</span>
                  </div>
                  <p class="text-sm text-ink-1 leading-relaxed mb-2">{work}</p>
                  <p class="font-mono text-[9px] uppercase tracking-[0.14em] text-ink-2">{band}</p>
                </div>
              ))}
            </div>
            <p class="mt-4 font-mono text-[10px] text-ink-2 leading-relaxed">
              FIG.4 · Six levels. From L4 upwards, enforcement at the boundary replaces observation as the qualifying evidence.
            </p>
          </div>
        </div>

      </div>
    </section>

    <!-- ══════════ WHY INDEPENDENCE MATTERS ══════════ -->
    <section class="border-t border-line-0 bg-bg-1">
      <div class="max-w-7xl mx-auto px-6 py-16 md:py-24 md:border-x md:border-line-0">
        <div class="grid md:grid-cols-2 gap-12 md:gap-0 md:divide-x md:divide-line-0 [&>*:first-child]:md:pr-12 [&>*:last-child]:md:pl-12 items-start">

          <div>
            <p class="font-mono text-xs uppercase tracking-widest text-white/30 mb-6">Why independence matters</p>
            <h2 class="text-3xl md:text-4xl font-bold text-white leading-tight mb-6">
              Trust measurements are currently produced within individual platforms.
            </h2>
            <p class="text-base text-white/45 leading-relaxed mb-4">
              Model providers observe their own environments. Enterprises observe only their own deployments. Neither provides an independent measure across providers, infrastructures and organizational boundaries.
            </p>
            <p class="text-base text-white/45 leading-relaxed">
              The execution boundary is the only location where every externally observable action can be measured using the same methodology. KYDE provides that common measurement layer.
            </p>
          </div>

          <div class="space-y-px">
            {[
              { who: "Model providers", note: "Observe activity within their own platforms.",                                               highlight: false },
              { who: "Enterprises",     note: "Observe their own deployments.",                                                            highlight: false },
              { who: "KYDE",            note: "Computes one independent Trust Score across providers, models and enterprise environments.", highlight: true  },
            ].map(({ who, note, highlight }) => (
              <div class={`p-5 border grid grid-cols-[140px_1fr] gap-4 items-start ${highlight ? 'border-warn/40 bg-warn/5' : 'border-white/8 bg-white/[0.02]'}`}>
                <span class={`font-mono text-[10px] uppercase tracking-widest shrink-0 pt-0.5 ${highlight ? 'text-warn' : 'text-white/25'}`}>{who}</span>
                <p class={`text-sm leading-snug ${highlight ? 'text-white/70' : 'text-white/35'}`}>{note}</p>
              </div>
            ))}
          </div>

        </div>
      </div>
    </section>

    <!-- ══════════ DEPLOYMENT ══════════ -->
    <section class="border-t border-white/5 bg-bg-1">
      <div class="max-w-7xl mx-auto px-6 py-16 md:py-24 md:border-x md:border-line-0">
        <div class="grid md:grid-cols-2 gap-12 md:gap-0 md:divide-x md:divide-line-0 [&>*:first-child]:md:pr-12 [&>*:last-child]:md:pl-12 items-center">

          <div>
            <p class="font-mono text-xs uppercase tracking-widest text-white/30 mb-6">Deployment</p>
            <h2 class="text-3xl md:text-4xl font-bold text-white leading-tight mb-6">
              Deployment requires a single endpoint change.
            </h2>
            <p class="text-white/45 leading-relaxed mb-6">
              No SDKs. No application changes. No framework dependency. Existing agents continue operating while every observable action becomes attributable, measurable and cryptographically recorded.
            </p>
            <p class="text-white/35 leading-relaxed text-sm">
              Supported environments include OpenAI, Anthropic, Gemini, self-hosted models and MCP-based architectures. Target latency: below 100 ms.
            </p>
          </div>

          <div class="dark-island border border-white/10">
            <div class="flex items-center gap-2 border-b border-white/8 px-4 py-3">
              <span class="h-2.5 w-2.5 rounded-full bg-alert/40"></span>
              <span class="h-2.5 w-2.5 rounded-full bg-warn/40"></span>
              <span class="h-2.5 w-2.5 rounded-full bg-live/40"></span>
              <span class="ml-2 font-mono text-[10px] text-white/20">terminal</span>
            </div>
            <div class="p-6 font-mono text-sm leading-loose overflow-x-auto">
              <p class="text-white/30 whitespace-nowrap"><span class="text-live">$</span> export OPENAI_BASE_URL=https://kyde.company/v1</p>
              <p class="text-white/30 mt-1"><span class="text-live">$</span> kyde fleet init</p>
              <p class="text-live mt-1">&#10003; Gateway active</p>
              <p class="text-live">&#10003; Trust Score initialized</p>
              <p class="text-live">&#10003; Cryptographic ledger started</p>
              <p class="text-white/30 mt-3"><span class="text-live">$</span> kyde fleet status</p>
              <p class="text-live mt-1">14 agents</p>
              <p class="text-live">Coverage: 94%</p>
              <p class="text-live">Trust Score: 87</p>
            </div>
          </div>

        </div>
      </div>
    </section>

    <!-- ══════════ CONTEXT ══════════ -->
    <section class="border-t border-line-0 bg-bg-1">
      <div class="max-w-7xl mx-auto px-6 py-16 md:py-24 md:border-x md:border-line-0">
        <div class="grid md:grid-cols-[160px_1fr] gap-8 md:gap-12 items-start max-w-4xl">
          <p class="font-mono text-xs uppercase tracking-widest text-ink-2">Context</p>
          <p class="text-base text-ink-1 leading-relaxed">
            Large-scale systems of trust have consistently relied on observable behavior rather than inferred intention. Credit assessment is based on repayment behavior. Telematics evaluates driving behavior. Financial audit evaluates recorded transactions. AI governance requires an equivalent behavioral measurement. The Trust Score applies the same principle to autonomous AI systems.
          </p>
        </div>
      </div>
    </section>

    <!-- ══════════ CTA ══════════ -->
    <section class="border-t border-line-0 bg-bg-0">
      <div class="max-w-7xl mx-auto px-6 py-16 md:py-24 md:border-x md:border-line-0">
        <div class="grid md:grid-cols-2 gap-6">

          <div class="bg-bg-1 p-8 md:p-10">
            <p class="font-mono text-xs uppercase tracking-widest text-white/30 mb-4">For enterprises</p>
            <h3 class="text-2xl font-bold text-white mb-4">Deploy the gateway and observe how Trust Score and Coverage evolve from the first recorded action.</h3>
            <p class="text-sm text-white/40 leading-relaxed mb-8">
              Kyde Gateway Starter is free. No SLA, no commitment. Self-hosted, deployed from GitHub in minutes.
            </p>
            <!-- Starter CTA intentionally disabled here — page CTAs stay Talk to Us (SBX-001 gate itself is lifted, navbar has Start Free)
            <a href="/starter" class="inline-flex items-center gap-2 bg-white text-cta-ink px-6 py-3.5 text-xs font-mono uppercase tracking-[0.12em] hover:bg-cta-hover transition-all">
              Deploy Starter →
            </a>
            -->
            <button
              onclick="openModal()"
              class="inline-flex items-center gap-2 bg-white text-cta-ink px-6 py-3.5 text-xs font-mono uppercase tracking-[0.12em] hover:bg-cta-hover transition-all"
            >
              Talk to Us →
            </button>
          </div>

          <div class="border border-line-0 bg-bg-1 p-8 md:p-10">
            <p class="font-mono text-xs uppercase tracking-widest text-ink-2 mb-4">For insurers, auditors and certification bodies</p>
            <h3 class="text-2xl font-bold text-ink-0 mb-4">Building the next generation of AI assurance frameworks?</h3>
            <p class="text-sm text-ink-1 leading-relaxed mb-8">
              The Trust Score provides an independent behavioral signal that can complement underwriting, assurance and certification processes. We welcome collaboration with organizations developing AI assurance infrastructure.
            </p>
            <a
              href="mailto:hello@kyde.com"
              class="inline-flex items-center gap-2 border border-line-1 text-ink-0 px-6 py-3.5 text-xs font-mono uppercase tracking-[0.12em] hover:bg-white hover:text-cta-ink transition-all"
            >
              hello@kyde.com →
            </a>
          </div>

        </div>

        <div class="mt-8 flex flex-wrap gap-4 text-xs text-ink-2">
          <a href="/blog/confidence-infrastructure-part-3" class="font-mono hover:text-ink-0 transition-colors underline underline-offset-4 decoration-ink-0/20">Technical Architecture →</a>
          <a href="/ai-agent-insurance" class="font-mono hover:text-ink-0 transition-colors underline underline-offset-4 decoration-ink-0/20">AI Agent Insurance Guide →</a>
          <a href="/blog/trust-is-a-number" class="font-mono hover:text-ink-0 transition-colors underline underline-offset-4 decoration-ink-0/20">Announcement →</a>
        </div>
      </div>
    </section>

  </main>
  <Footer />
</Base>
